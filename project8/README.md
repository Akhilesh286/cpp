## Problem 1: Basic Class Definition

**Question:**  
Create a class `Car` with attributes like `brand`, `model`, and `year`. Define a method `displayInfo()` to print the car details. Instantiate two objects of the `Car` class and call the method for each object.

For solutions [click here](/project8/solutions/p1.cpp).

---

## Problem 2: Constructor & Destructor

**Question:**  
Define a class `Person` with a constructor that takes `name` and `age` as parameters and initializes the object. Create a destructor that prints a message when the object is destroyed. Create objects to test the constructor and destructor.

For solutions [click here](/project8/solutions/p2.cpp).

---

## Problem 3: Private and Public Members

**Question:**  
Create a class `Rectangle` with private attributes `length` and `width`. Implement public methods `setDimensions()` and `getArea()`. Use these methods to set the dimensions and calculate the area of the rectangle.

For solutions [click here](/project8/solutions/p3.cpp).

---

## Problem 4: Friend Function

**Question:**  
Define a class `Box` with private members `height`, `width`, and `depth`. Create a friend function `calculateVolume()` that calculates the volume of the box.

For solutions [click here](/project8/solutions/p4.cpp).

---

## Problem 5: Copy Constructor

**Question:**  
Write a class `Book` that has attributes like `title` and `author`. Implement a copy constructor to copy the details from one object to another. Test it by creating two `Book` objects.

For solutions [click here](/project8/solutions/p5.cpp).

---

## Problem 6: Static Members

**Question:**  
Create a class `Student` with a static member `totalStudents` that keeps track of the total number of student objects created. Each time an object is created, update the static variable. Print the total number of students.

For solutions [click here](/project8/solutions/p6.cpp).

---

## Problem 7: Inheritance

**Question:**  
Define a base class `Animal` with a method `sound()` that prints a general message. Derive two classes `Dog` and `Cat` from `Animal`, and override the `sound()` method in both classes to print specific sounds. Instantiate objects of each derived class and call `sound()`.

For solutions [click here](/project8/solutions/p7.cpp).

---

## Problem 8: Class with Getters and Setters

**Question:**  
Create a class `Book` that represents a book with private attributes for `title` and `author`. Implement public methods to set and get these attributes using setters and getters. Instantiate a `Book` object, set its title and author, and display the details using the getter methods.

For solutions [click here](/project8/solutions/p8.cpp).

---

## Problem 9: Class with Composition

**Question:**  
Define a class `Address` that contains attributes for `street` and `city`. Then, create a class `Person` that has a name and an `Address` object as attributes. Implement a method in the `Person` class to display the person's name and address. Create an instance of `Person`, set its name and address, and print the information.

For solutions [click here](/project8/solutions/p9.cpp).

---

## Problem 10: Aggregation (Has-a Relationship)

**Question:**  
Define a class `Engine` with attributes like `horsepower`. Create a class `Car` that has an `Engine` object as a member. Set the engine’s horsepower and display it through the `Car` class.

For solutions [click here](/project8/solutions/p10.cpp).

---
